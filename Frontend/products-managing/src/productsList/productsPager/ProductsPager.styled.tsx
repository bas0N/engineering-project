import {styled} from 'styled-components';

export const ProductsPagerWrapper = styled.div`
    width: calc(60% - 20px);
    padding: 10px;
    display: flex;
    flex-direction: row;
    gap: 8px;
    align-items: center;
    justify-content: center;
`;