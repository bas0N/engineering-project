import styled from "styled-components";
import {CardPreview} from '@fluentui/react-components';

export const AdminLoginPanel = styled(CardPreview)`
    padding: 10px;
    display: flex;
    flex-direction: column;
    gap: 8px;
`;