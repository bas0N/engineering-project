import styled from "styled-components";

export const AppWrapper = styled.div`
    width: calc(100% - 20px);
    padding: 10px;
    display: flex;
    flex-direction: column;
    gap: 40px;
    align-items: center;
`;
