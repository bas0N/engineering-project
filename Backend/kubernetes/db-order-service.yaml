apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.34.0 (HEAD)
  labels:
    io.kompose.service: db-order
  name: db-order
spec:
  ports:
    - name: "5429"
      port: 5429
      targetPort: 5432
  selector:
    io.kompose.service: db-order
