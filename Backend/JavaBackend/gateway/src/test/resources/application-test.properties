# JWT Configuration for Tests
jwt.secret=TEST_JWT_SECRET

# MongoDB Configuration for Tests (if needed)
spring.data.mongodb.uri=mongodb://localhost:27017/test_db

# Cloudinary Configuration for Tests (if needed)
cloudinary.cloud_name=test_cloud
cloudinary.api_key=test_api_key
cloudinary.api_secret=test_api_secret
